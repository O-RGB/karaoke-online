// import { create } from "zustand";

// interface TickStore {
//   tick: number;
//   setCurrntTick: (tick: number) => void;
// }

// const useTickStore = create<TickStore>((set) => ({
//   tick: 0,
//   setCurrntTick: (tick) =>
//     set((state) => ({
//       tick,
//     })),
// }));

// export default useTickStore;
